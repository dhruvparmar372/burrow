# ScaleTails

Deploy tailscale exit nodes in AWS region(s) of your choice.


## Next Steps
- Use map variable & lookup for saving ami ids. Move ami_ids to common variable file
- Add makefile to deploy a specific region
